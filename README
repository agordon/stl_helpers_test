This tiny project users "stl_helpers" as a git submodule.

Git Submodules help:
  http://git-scm.com/book/en/Git-Tools-Submodules

It was created with:
  $ git init
  $ git submodule add git://github.com/agordon/stl_helpers.git
  $ git commit
  $ git add Makefile test.cpp .gitignore
  $ git commit

When cloning this project:
 do:
   $ git clone git://github.com/agordon/stl_helpers_test.git
   $ cd stl_helpers_test
   $ git submodule init
   $ git submodule update
 OR
   $ git clone --recursive git://github.com/agordon/stl_helpers_test.git

